I'm mainly referencing the Google C++ style guide found here : https://google.github.io/styleguide/cppguide.html#Naming

folder names : lowercase with dashes
file names (code): uppercase and dashes
file names (other) : lowercase and dashes

class, struct, types alias, enum, and template names : pascal case
functions : pascal case
functions (accessors) : camel case

enum members : same as constants
constants : leading k with snake case following

variables : snake case
private class members: snake case with a trailing underscore

macros : caps lock snake case