//
// Created by svjat on 8/10/2025.
//

#include <gtest/gtest.h>
#include "../core/Chunk.h"

TEST(ChunkTest, CalculateIndex)
{
	// Chunk chunk(...); // your constructor args
	// EXPECT_EQ(chunk.CalculateIndex(0,0,0), 0);
	// EXPECT_EQ(chunk.CalculateIndex(1,0,0), chunk.k_chunk_size_y_ * chunk.k_chunk_size_z_);
}